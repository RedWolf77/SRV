{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cmake build",
            "type": "shell",
            "command": "cd build && make",
            "group": "build",
            "problemMatcher": ["$gcc"]
        }
    ]
}